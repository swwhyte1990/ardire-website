kind = "web"
previewPath = "/"
title = "Ardire"
version = "1.0.0"
id = "artifacts/ardire"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 18439

[services.development]
run = "pnpm --filter @workspace/ardire run dev"

[services.production]

[services.production.build]
args = ["sh", "-c", "pnpm --filter @workspace/ardire run build && pnpm --filter @workspace/api-server run build"]

[services.production.build.env]
NODE_ENV = "production"
BASE_PATH = "/"

[services.production.run]
args = ["node", "--enable-source-maps", "artifacts/api-server/dist/index.mjs"]

[services.production.run.env]
PORT = "18439"
NODE_ENV = "production"

[services.production.health.startup]
path = "/api/healthz"

[services.env]
PORT = "18439"
BASE_PATH = "/"
